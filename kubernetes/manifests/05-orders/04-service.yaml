apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: retail
  labels:
    app: orders
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: orders
