apiVersion: v1
kind: Service
metadata:
  name: checkout
  namespace: retail
  labels:
    app: checkout
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: checkout
