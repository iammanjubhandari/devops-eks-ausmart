apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: retail
  labels:
    app: cart
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: cart
