apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: retail
  labels:
    app: catalog
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: catalog
